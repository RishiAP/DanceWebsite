touch .gitignore
git config --global user.name "Rishi"
git config --global user.email "debjyotim2019@gmail.com"
git init
git add .
git commit -m "Initial commit"
git status # just to see
ssh-keygen -t ed25519 -C "debjyotim2019@gmail.com"
tail <path to id_rsa.pub> # add this key to github account
git remote add origin <ssh url.git>
git push origin master